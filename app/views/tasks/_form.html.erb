<%= form_with(model: task, local: true) do |f| %>

  <%= f.label :status, '進捗状況' %>
  <%= f.text_field :status %>

  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content %>
  
  <%= f.submit '投稿' %>
<% end %>


